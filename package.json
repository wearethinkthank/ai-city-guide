{
  "name": "tripbot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "poststart": "if [ \"$RUN_DB_PUSH\" = \"1\" ]; then npx prisma db push; fi",
    "prisma:gen": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.16.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^6.11.2",
    "openai": "^4.58.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "prisma": "^5.16.0"
  }
}
